import 'package:flutter/material.dart';

class OliverUI extends StatelessWidget {
  const OliverUI({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Ebbes',
      home: Container(),
    );
  }
}
